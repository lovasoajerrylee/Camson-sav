<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}EcamStock!{% endblock %}</title>
    <link rel="icon" href="{{ asset('asset/img/fiuyegetpmzc.png')}}">
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('asset/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.1/font/bootstrap-icons.css">
        <link rel="stylesheet" href="{{ asset('asset/css/style.css') }}"/>
        <style>
        .header {
         padding: 20px;
         text-align: center;
         background: #FF0085;
         color: white;
         font-size: 30px;
        }
        
        .userLookConnected
        {
            position: relative;
            right: 15px;
            top: 19px;
        }

        </style>
        {# {{ encore_entry_link_tags('app') }} #}
    {% endblock %}

</head>

<body>  
        <div id="iCamStock-title" class="header" text-align: center>
           <img id="logo" width="40px" href="#" class="mx-1" src="{{ asset('asset/img/zeltjfaswjqr.png') }}">
           <a href="/">             
                <span class="col-md-3 ms-auto text-white">Service Aprés Vente</span>
            </a>
        </div>
        {# user look #}
    {% if app.user %}
        <div class="position-absolute top-0 end-0" id="user-toogle">
            <button class="btn btn-secondary dropdown-toggle userLookConnected" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" area-expanded="false">
                <i class="bi bi-person"></i> <span></span>
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                <li>
                    <a class="dropdown-item" data-toggle="modal" data-target="#createmodel0" href="#" href="#"> <i class="bi bi-person"></i> Profile</a>
                </li>
                                <li>
                    <a class="dropdown-item"  href="#"> <i class="bi bi-person"></i> Profile</a>
                </li>
                <li>
                    <a class="dropdown-item"  href="{{ path('app_logout') }}"><i class="bi bi-power"></i> Deconnexion</a>
                </li>                    
            </ul>
        </div>
    {% endif %}

{% block body %}
    <!-- debut main -->

    <!-- fin main -->
{% endblock %}
</div>
</body>
{% for message in app.flashes('error') %}
<div class="alert alert-danger">
    {{ message }}
</div>
{% endfor %}
{% block javascripts %}
    {# {{ encore_entry_script_tags('app') }} #}
{% endblock %}
<script src="{{ asset('asset/js/jquery.min.js') }}"></script>
<script src="{{ asset('asset/js/bootstrap.bundle.min.js') }}"></script>
<script src="{{ asset('asset/js/photos.js') }}"></script>
<script src="{{ asset('asset/js/app.js') }}"></script>
<script type="text/javascript" src="{{ asset('asset/js/popper.min.js') }}"></script>
<script type="text/javascript">
    $(function () {
        $("#toggle_pwd").click(function () {
            $(this).toggleClass("bi bi-eye-slash");
            var type = $(this).hasClass("bi-eye-slash") ? "text" : "password";
            $("#inputPassword").attr("type", type);
        });
    });
</script>
</html>